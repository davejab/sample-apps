apiVersion: v1
kind: Service
metadata:
  name: {{ include "details.name" . }}
  labels:
    {{- include "details.labels" . | nindent 4 }}
spec:
  ports:
  - port: 9080
    name: http
  selector:
    {{- include "details.selectorLabels" . | nindent 4 }}
    version: v1