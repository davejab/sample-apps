apiVersion: v1
kind: Service
metadata:
  name: {{ include "ratings.name" . }}
  labels:
    {{- include "ratings.labels" . | nindent 4 }}
spec:
  ports:
  - port: 9080
    name: http
  selector:
    {{- include "ratings.selectorLabels" . | nindent 4 }}