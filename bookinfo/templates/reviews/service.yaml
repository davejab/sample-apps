apiVersion: v1
kind: Service
metadata:
  name: {{ include "reviews.name" . }}
  labels:
    {{- include "reviews.labels" . | nindent 4 }}
spec:
  ports:
  - port: 9080
    name: http
  selector:
    {{- include "reviews.selectorLabels" . | nindent 4 }}